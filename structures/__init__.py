"""Structural science layer: molecules and crystals as content-addressed
scientific STRUCTURE, upstream of computation.

A structure is neither a computation, an operation, a warrant, nor
evidence -- it is the scientific object a computation is ABOUT. The five
identities stay separate:

    STRUCTURE    ste.structure.*        this package
    COMPUTATION  ExecutionSpecification unchanged (structures LOWER into it)
    OPERATION    OperationTrace         unchanged (occurrence-distinct)
    WARRANT      WarrantCache           unchanged (proof bytes, re-verified)
    EVIDENCE     EvidencePool           unchanged (content-collapsing)
"""
